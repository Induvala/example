import React from "react";
import LoginBox from "../features/auth/components/organisms/LoginBox";

export default function LoginPage() {
  return (
    <div className="flex overflow-hidden
 min-h-screen items-center justify-center bg-gradient-to-r from-yellow-50 to-white">
      <LoginBox />
    </div>
  );
}
