import React from "react";

const ChatSupportButton: React.FC = () => {
    return (
        <button
            className="fixed bottom-6 right-6 w-12 h-12 bg-yellow-400 text-black font-bold rounded-full flex items-center justify-center shadow-lg hover:bg-yellow-500 transition"
            aria-label="Chat Support"
        >
            Ch
        </button>
    );
};

export default ChatSupportButton;
